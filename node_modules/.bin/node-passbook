#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../passbook/bin/node-passbook" "$@"
  ret=$?
else 
  node  "$basedir/../passbook/bin/node-passbook" "$@"
  ret=$?
fi
exit $ret
